name: Publicar GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-22.04
    environment:
      name: github-pages

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: # 2️⃣ Preparar conteúdo do Pages

  run: |
    echo "🧹 Limpando pasta docs/"
    rm -rf docs
    mkdir docs

    echo "📄 Copiando dashboard"
    cp -r pages/* docs/

    echo "📦 Copiando datapackage.json"
    if [ ! -f datapackage/datapackage.json ]; then
      echo "❌ datapackage/datapackage.json não encontrado"
      exit 1
    fi
    cp datapackage/datapackage.json docs/datapackage.json

    echo "📂 Conteúdo final do docs/"
    ls -lah docs


      - name: 📦 Upload artefato Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: 🚀 Deploy no GitHub Pages
        uses: actions/deploy-pages@v4



